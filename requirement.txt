cd "C:\Users\rijes\OneDrive\Desktop\HackathonBirthamode"

py -3.13 -m venv .venv

.\.venv\Scripts\Activate.ps1

python -m pip install --upgrade pip

if (Test-Path "requirements.txt") {
    python -m pip install -r requirements.txt
} else {
    python -m pip install Flask Pillow ImageHash PyWavelets
}

python app.py